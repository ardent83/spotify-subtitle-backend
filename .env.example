----------------------------------
CORE DJANGO SETTINGS
----------------------------------
IMPORTANT: Generate a new secret key for your environment.
You can generate one by running: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=
DEBUG=True

----------------------------------
DATABASE (PostgreSQL)
----------------------------------
DB_NAME=spotify_project_db
DB_USER=spotify_user
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

----------------------------------
SPOTIFY API CREDENTIALS
----------------------------------
Get these from your Spotify Developer Dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

----------------------------------
SECURITY & CORS (Development Defaults)
----------------------------------
For production, replace these with your actual domains
ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

----------------------------------
DJANGO-ALLAUTH SETTINGS
----------------------------------
a welcome page
LOGIN_REDIRECT_URL=http://localhost:8000/welcome